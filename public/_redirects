# Cloudflare Pages / Workers Redirects
# When using Cloudflare Workers with [assets] and not_found_handling = "single-page-application",
# do NOT add /* /index.html 200 here as Cloudflare Asset router flags it as an infinite redirect loop (code: 100324).

