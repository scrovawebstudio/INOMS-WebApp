# Exclude API endpoints from static index.html rewrite to prevent HTTP 405 Method Not Allowed
/api/*  /api/:splat  200

# Cloudflare Pages SPA fallback: routes remaining non-API page requests to index.html
/*      /index.html  200

